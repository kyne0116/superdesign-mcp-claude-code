# Superdesign MCP Server Environment Configuration
# Copy this file to .env and modify the values according to your setup

# =============================================================================
# 基本配置 / Basic Configuration
# =============================================================================

# MCP服务器名称 / MCP Server Name
MCP_SERVER_NAME=superdesign

# MCP服务器版本 / MCP Server Version  
MCP_SERVER_VERSION=1.0.0

# =============================================================================
# 路径配置 / Path Configuration
# =============================================================================

# 工作目录路径 / Workspace Directory Path
# 默认使用当前目录，可以设置为绝对路径
# Default: current directory, can be set to absolute path
# 
# Windows 示例 / Windows Example:
# WORKSPACE_PATH=D:\Projects\MyDesigns
# 
# macOS/Linux 示例 / macOS/Linux Example:
# WORKSPACE_PATH=/Users/username/Projects/MyDesigns
# WORKSPACE_PATH=/home/username/Projects/MyDesigns
WORKSPACE_PATH=

# Superdesign输出目录名称 / Superdesign Output Directory Name
# 相对于工作目录的子目录名称
# Subdirectory name relative to workspace path
SUPERDESIGN_DIR_NAME=superdesign

# 设计迭代目录名称 / Design Iterations Directory Name
DESIGN_ITERATIONS_DIR=design_iterations

# 设计系统目录名称 / Design System Directory Name  
DESIGN_SYSTEM_DIR=design_system

# =============================================================================
# MCP配置路径 / MCP Configuration Paths
# =============================================================================

# MCP服务器可执行文件路径 / MCP Server Executable Path
# 用于生成MCP配置文件的路径
# Path used for generating MCP configuration files
#
# Windows 示例 / Windows Example:
# MCP_SERVER_PATH=D:\02_Dev\Workspace\GitHub\superdesign-mcp-claude-code\dist\index.js
#
# macOS/Linux 示例 / macOS/Linux Example:  
# MCP_SERVER_PATH=/Users/username/superdesign-mcp-claude-code/dist/index.js
# MCP_SERVER_PATH=/home/username/superdesign-mcp-claude-code/dist/index.js
MCP_SERVER_PATH=

# Node.js可执行文件路径 / Node.js Executable Path
# 通常可以留空使用系统PATH中的node
# Usually can be left empty to use node from system PATH
#
# Windows 示例 / Windows Example:
# NODE_EXECUTABLE=C:\Program Files\nodejs\node.exe
#
# macOS/Linux 示例 / macOS/Linux Example:
# NODE_EXECUTABLE=/usr/local/bin/node
NODE_EXECUTABLE=node

# =============================================================================
# 服务器配置 / Server Configuration  
# =============================================================================

# 实时画廊服务器默认端口 / Live Gallery Server Default Port
LIVE_GALLERY_PORT=3000

# HTTP服务器超时时间（毫秒）/ HTTP Server Timeout (milliseconds)
HTTP_TIMEOUT=30000

# =============================================================================
# 文件管理配置 / File Management Configuration
# =============================================================================

# 默认设计变体数量 / Default Design Variations Count
DEFAULT_VARIATIONS=3

# 最大设计变体数量 / Maximum Design Variations Count
MAX_VARIATIONS=5

# 清理功能默认保留天数 / Cleanup Default Retention Days
DEFAULT_CLEANUP_DAYS=30

# 清理功能默认保留文件数量 / Cleanup Default File Count
DEFAULT_CLEANUP_COUNT=50

# 最大文件大小（字节）/ Maximum File Size (bytes)
# 10MB = 10485760 bytes
MAX_FILE_SIZE=10485760

# =============================================================================
# 调试和日志配置 / Debug and Logging Configuration
# =============================================================================

# 启用调试模式 / Enable Debug Mode
# 设置为 true 启用详细日志输出
# Set to true to enable verbose logging
DEBUG=false

# 日志级别 / Log Level
# 可选值: error, warn, info, debug
# Options: error, warn, info, debug
LOG_LEVEL=info

# 启用性能监控 / Enable Performance Monitoring
ENABLE_PERFORMANCE_MONITORING=false

# =============================================================================
# 平台特定配置 / Platform Specific Configuration
# =============================================================================

# 强制指定平台类型 / Force Platform Type
# 通常自动检测，可选值: win32, darwin, linux
# Usually auto-detected, options: win32, darwin, linux
FORCE_PLATFORM=

# Windows特定：使用长路径支持 / Windows Specific: Use Long Path Support
# 在Windows上启用长路径支持（需要Windows 10 1607+）
# Enable long path support on Windows (requires Windows 10 1607+)
WINDOWS_LONG_PATH_SUPPORT=true

# macOS/Linux特定：文件权限 / macOS/Linux Specific: File Permissions
# 新创建文件的权限模式
# Permission mode for newly created files
FILE_PERMISSIONS=0644

# 新创建目录的权限模式 / Permission mode for newly created directories
DIR_PERMISSIONS=0755

# =============================================================================
# 高级配置 / Advanced Configuration
# =============================================================================

# 自定义环境变量前缀 / Custom Environment Variable Prefix
# 用于避免与其他应用的环境变量冲突
# Used to avoid conflicts with other applications' environment variables
ENV_PREFIX=SUPERDESIGN_

# 配置文件监听 / Configuration File Watching
# 启用配置文件变更监听和热重载
# Enable configuration file change watching and hot reload
ENABLE_CONFIG_WATCHING=false

# 临时文件目录 / Temporary Files Directory
# 留空使用系统默认临时目录
# Leave empty to use system default temp directory
TEMP_DIR=

# 备份配置 / Backup Configuration
# 启用自动备份功能
# Enable automatic backup functionality
ENABLE_AUTO_BACKUP=false

# 备份保留数量 / Number of backups to retain
BACKUP_RETENTION_COUNT=5
